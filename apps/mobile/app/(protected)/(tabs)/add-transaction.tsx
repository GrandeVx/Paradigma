import { View } from "react-native";

// This is a dummy screen that should never be rendered
// The actual navigation is handled by the TabBar component
export default function AddTransactionScreen() {
  return <View />;
}